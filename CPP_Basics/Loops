#include<iostream>
using namespace std;

int main(){

    // Loops are used to execute repetative tasks until a certain condition is met.

    // // For Loop

    // for(int i=0;i<10;i++){ // for loops is used when we know the number of iterations in advance.
    //     cout<<i<<endl;
    // }

    // // while loop

    // int j=0;

    // while(j<10){ // while loop is used when we do not know the number of iterations in advance and we want to execute the loop until a certain condition is met.
    //     cout<<j<<endl;
    //     j++;
    // }

    // // do-while loop

    // int k=0;

    // do{  // do-while loop is similar to while loop but it executes the loop at least once even if the condition is false because the condition is checked after the loop body is executed.
    //     cout<<k<<endl;
    //     k++;
    // }
    // while(k<10);


    // // break and continue statements

    // for(int i=0;i<10;i++){

    //     if(i==5){
    //         break; // break statement is used to exit the loop when a certain condition is met.
    //     }
    //     cout<<i<<endl;
    // }

    // for(int i=0;i<10;i++){

    //     if(i==5){
    //         continue; // continue statement is used to skip the current iteration of the loop when a certain condition is met and continue with the next iteration of the loop.
    //     }
    //     cout<<i<<endl;
    // }

    // // nested Loops

    // for(int i=0;i<3;i++){
    //     for(int j=0;j<3;j++){
    //         cout<<"i: "<<i<<" j: "<<j<<endl;
    //     }
    // }

}